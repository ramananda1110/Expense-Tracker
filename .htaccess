<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect all requests to the public folder
    RewriteCond %{REQUEST_URI} !^/Expense-Tracker/public/
    RewriteRule ^(.*)$ /Expense-Tracker/public/$1 [L]

</IfModule>
